export const btnsSwipperState = (st)=>(
    {
    type: 'ADD_BTN_STATE',
    stat: st
}
);

export const PreloadState = (st)=>(
   
        {
            type: 'ADD_PRELOAD_STATE',
            stat: st
        }
);